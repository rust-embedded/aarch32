[package]
name = "aarch32-tests"
version = "0.4.0"
edition = "2024"
publish = false
description = "Qemu and insta based test suite"

[[test]]
name = "tests"
harness = false

[dev-dependencies]
insta = { version = "1", features = ["filters"] }
serde_json = "1"
libtest-mimic = "0.8"
