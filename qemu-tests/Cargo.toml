[package]
name = "qemu-tests"
version = "0.4.0"
edition = "2024"
publish = false
description = "Qemu and insta based test suite"

[dev-dependencies]
insta = { version = "1", features = ["filters"] }
serde_json = "1"
